class : 
    Server:
        int fd;
        Irc channel[max_channel];
        ...
    Irc(password):
        int fd;
        User user[max_user_par_channel]
        
    Clients 
        int fd;
        string ip;
    User:
        string nickname;
        string hostname;
        string username;
        ...

    ;
le Serveur est cree sur un port et un password;
un IRC/CHANNEL est instancié avec le password();

while (true)
{
nouvelle connection au serveur = new client(fd, ip); 
if (client sends irc.password)
    irc.adduser(fdClient, new User());
else
    kick client
}
}